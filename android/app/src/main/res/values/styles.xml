<resources>

    <!-- Tema base -->
    <style name="AppTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores de las barras -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>

        <!-- Que el texto en la status bar sea claro (blanco) -->
        <item name="android:windowLightStatusBar">false</item>

        <!-- Que los iconos de la barra de navegación sean claros -->
        <item name="android:windowLightNavigationBar">false</item>

        <!-- Que la app NO sea fullscreen y respete los límites -->
        <item name="android:windowTranslucentStatus">false</item>
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- Tema sin ActionBar para MainActivity -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>

        <!-- Fondo blanco sólido -->
        <item name="android:windowBackground">@android:color/white</item>

        <!-- Barras -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/white</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- No extender debajo -->
        <item name="android:windowFullscreen">false</item>
    </style>

    <!-- Tema para Launch/Splash -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:background">@drawable/splash</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
